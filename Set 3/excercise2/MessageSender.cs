using System;
using System.Net;
using System.Net.Mail;

namespace Ex
{
    public class MessageSender
    {
        //sends messages to a worker that handles asynchronous tasks 
        public async Task SendAsync(string messageName, object payload)
        {
            //...//
        }
    }
}
